const body = document.body
const div = document.querySelector('div')
const spanHi = document.querySelector('#hi')
const spanBye = document.querySelector('#bye')

spanHi.style.backgroundolor = "red"

// spanHi.classList.toggle("hi3", true)

// spanHi.classList.remove("new")

// spanHi.classList.add("new")

// spanHi.dataset.newName = "hi"

// console.log(spanHi.dataset.longerName)

// spanHi.removeAttribute('title')

// spanHi.id = "sdfsdf"

// console.log(spanHi.setAttribute('title', "sdfref"))

// console.log(spanHi.id)

// console.log(spanHi.getAttribute('title'))

// console.log(spanHi.getAttribute())

// console.log(spanHi.getAttribute('title'))

// spanBye.remove()//remive 'Bye from webpage'
// div.append(spanBye)//It add removed item back

// spanHi.remove()
// div.removeChild(spanHi) //above and this both works same, but above is easy




// const div = document.createElement("div")
//div.innerText = "Hello world"
// div.innerHTML = "<strong>Hello World 2</strong>" // this is the only way in javascript where you  can actually add html from a string into an element
// body.append(div)

// const strong = document.createElement("strong") // this is the another way you can add style to it 
// strong.innerText = ("Hello World")
// body.append(strong)













// const div = document.querySelector('div')
// console.log(div.textContent)
// console.log(div.innerText)